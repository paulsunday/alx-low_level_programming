#include "main.h"
/**
* print_alphabet->prints the lowercase alphabets
*
* Return: Always zero (Success)
*/
void print_alphabet(void)
{
int ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
_putchar(ch);
}
_putchar('\n');
}
