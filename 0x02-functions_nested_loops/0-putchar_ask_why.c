#include "main.h"
/**
* Main- Prints is the entry point always at the begining of the page
* Return: Always zero (Success)
*/
int main(void)
{
  char *ch;
  ch = "_putchar";
putchar(*ch);
putchar('\n');
return (0);
}
