#include "main.h"
/**
* print_alphabet-> Prints Alphabets 10 times
*
* Return: Always Zero (Success)
*/
void print_alphabet_x10(void)
{
int ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
_putchar(ch);
}
int print_aphabet_x10 = (ch * 10);
_putchar('\n');
}
