#include <stdio.h>
#include <stdlib.h>
/**
* main- Is the entry point of the program
* Return:Is always Zero (Success)
*/
int main(void)
{
int ch = 'z';
while (ch >= 'a')
putchar(ch);
ch++;
putchar('\n');
return (0);
}
