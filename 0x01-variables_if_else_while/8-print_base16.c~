#include <stdio.h>
#include <stdlib.h>
/**
* main-Entry point of the program
* return: Always zero (Success)
*/
int main(void)
{
int n;
char ch;
for (n = 0; n <= 9; n++)
putchar((n % 9) + 0);
for (ch = 'a'; ch <= 'f'; ch++)
putchar(ch);
putchar('\n');
return (0);
}
